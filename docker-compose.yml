services:
  # Python flask app
  python-app:
    build: .
    ports:
      - "5555:5555"
    restart: unless-stopped
    environment:
      - DATABASE_URL=postgres://survey_app:${SURVEY_APP_PASSWORD}@db-app:5432/db-app?sslmode=disable
      - FLASK_SECRET_KEY=${FLASK_SECRET_KEY}
    depends_on:
      - db-migrator
    command: ["gunicorn", "-w", "4", "-b", "0.0.0.0:5555", "app:app"]

  # Postgres database
  db-app:
    image: postgres:16.14-alpine3.23
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: db-app
      SURVEY_APP_PASSWORD: ${SURVEY_APP_PASSWORD}
    volumes:
      - db_data:/var/lib/postgresql/data
      - ./init-scripts:/docker-entrypoint-initdb.d
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d db-app"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Database migration tool
  db-migrator:
    image: amacneil/dbmate:2.33
    environment:
      DATABASE_URL: "postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db-app:5432/db-app?sslmode=disable"
      DBMATE_MIGRATIONS_DIR: "/db-migrations"
    volumes:
      - ./db-migrations:/db-migrations
    depends_on:
      db-app:
        condition: service_healthy
    command: ["up"]

volumes:
  db_data:
